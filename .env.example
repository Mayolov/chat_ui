# Optional: Pre-configure defaults so the UI launches ready to use.
# These are only used as initial defaults — all settings can be changed in the UI.

# CHAT_API_KEY=your-api-key-here
# CHAT_API_BASE=https://integrate.api.nvidia.com
# CHAT_MODEL=nvidia/llama-3.1-nemotron-70b-instruct
